export enum SidebarActions {
  AllResolved = "Mark all as resolved",
  AllUnresolved = "Mark all as unresolved",
  RemoveAllTodos = "Remove all todos",
  AddTodo = "Add a todo",
}
export enum FilterTypes {
  Color = "color",
  Unresolved = "unresolved",
  Resolved = "resolved",
}
